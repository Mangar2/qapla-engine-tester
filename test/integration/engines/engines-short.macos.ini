# Best-effort template: no macOS binaries were available to verify these paths
# against. Adjust the cmd= values to match your actual macOS builds; the
# engine names must stay as they are, other fixtures reference them by name.
[engine]
name=Spike 1.4
proto=uci
cmd=test/integration/engines/Spike1.4.1-macos
option.OwnBook=false

[engine]
name=Qapla 0.4.0
proto=uci
cmd=test/integration/engines/Qapla0.4.0-macos

[engine]
name=qai_pla_uci
proto=uci
cmd=test/integration/engines/qai_pla_uci
